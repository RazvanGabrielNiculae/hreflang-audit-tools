# Hreflang audit model

Audit language identity, canonical consistency and alternate declarations together. For site-wide validation, add reciprocal-pair and sitemap checks rather than interpreting one page in isolation.
